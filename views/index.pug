html
  head
    title MapSaw (a demo of node express pug & maptiler)
    // ++ Google Tag Manager Head
    script(src='dynamic/js/gtaghead.js')
    // -- Google Tag Manager Head
    script(src='dragdrop.js')
  body
    // ++ Google Tag Manager Body
    noscript
      iframe(src=mygtmurl height="0" width="0" style="display:none;visibility:hidden")
    // -- Google Tag Manager Body
    h1 MapSaw (a demo of node express pug & maptiler)
    p map tiles from 
    a(href='https://tileserver.maptiler.com/nasa/') maptiler.com
    
    - var id = 100
    
    p DEST
    table(style="border-collapse:collapse")
      each r in images1
        tr
          each c in r
            td(style="padding:0")
              if c
                img(id=id++ src=c height=80 width=80)
              else
                img(id=id++ src='' height=80 width=80
                  ondragover="on_dragover(event)"
                  ondrop="on_drop(event)"
                ) 
    p SOURCE 
    table(style="border-collapse:collapse")
      each r in images2
        tr
          each c in r
            td
              img(id=id++ src=c height=80 width=80 
                ondragstart="on_dragstart(event)"
              )

    // include the Glitch button to show what the webpage is about 
    // and to make it easier for folks to view source and remix
    div(class="glitchButton" style="position:fixed;top:20px;right:20px;")
    script(src="https://button.glitch.me/button.js")
    